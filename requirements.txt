# Dependances pour executer le pipeline en local (hors conteneurs Airflow).
# Dans Docker, ces paquets sont installes via le Dockerfile.
requests==2.32.5
beautifulsoup4==4.12.3
lxml==5.3.0
hdfs==2.7.3
pymongo==4.10.1
python-dotenv==1.0.1
